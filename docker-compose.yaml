version: '3'

services:
  app:
    build: .
    ports:
      - "5000:5000"  # Подставьте нужные вам порты
    environment:
      - FLASK_APP=app.py  # Замените на имя вашего приложения
      - FLASK_RUN_HOST=0.0.0.0
    depends_on:
      - db
    links:
      - db
  db:
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: yphau110
      MYSQL_DATABASE: pr
    ports:
      - "3307:3306"  # Подставьте нужный вам порт
